# 引継ぎメモ（HANDOFF）

次のチャットや別の日に作業を再開するとき用です。  
**APIキーやパスワードは書かないでください。**

---

## 新しいチャットへの貼り付け用（短文）

```
【引継ぎ】Reflective AI

リポジトリ: https://github.com/izumi-miysa/Reflective-AI
公開: https://reflective-ai-red.vercel.app
アプリ: web/（Next.js）
設計の正: Logs/Discussion-2026-08-08.md（構造）／Logs/Discussion-2026-08-12.md（Why）／Logs/Discussion-2026-08-14.md（実装）／Logs/Discussion-2026-08-15.md（Why実例・誘導・続ける）／Logs/Discussion-2026-08-17.md（入口は場の約束）

いまの状態:
- 入口は「ここは、答えを出さない場所です」。誘い「誰かに話したいこと、聞いてもらいたいことはありませんか？」。筋書き（このあと誰と話すか）は出さない。ボタンは「話してみる」
- 話す相手は本人が選ぶ。候補ボタンは薄い紺（濃い紺の主ボタンにしない＝誘導しない）
- 「もう少し続ける」＝書く画面に戻る（文は残す。AIは書いているあいだ黙る。書きました、でもう一度だけ返す。2回目のあとはボタンを出さない）
- ステージ途中の誘い: 「今のやり取りを、少し離れたところから返してもらいませんか」／返してもらう／もう少し話す
- 評価者向けWhy実例は docs/evaluator-why-example.md（返すこと自体は汎用AIにもできる。差は返して止まること）
- Googleフォームは docs/demo-survey.md に合わせて更新済み。ログインなし送信を確認済み

次にやること:
1. 知人テストを続ける。感想は公開中のフォームへ

参照: @HANDOFF.md @Logs/Discussion-2026-08-17.md @Logs/Discussion-2026-08-15.md @docs/evaluator-why-example.md @docs/demo-survey.md
（発表のWhyが要るときだけ @docs/WHY-ja.md。入口には載せない）
```

---

## 引継ぎのコツ

1. **新しい Agent チャットを開き、上の短文を最初に貼る**  
   （前のチャットは文脈をほぼ引き継がないため）
2. 可能なら `@ファイル名` で関連ファイルを指定する
3. **APIキーは貼らない**（`.env.local` と Vercel の環境変数だけ）
4. 「いまの状態」「次にやること」を1〜3行で書くと再開が速い
5. この `HANDOFF.md` 自体も、区切りがついたら「いまの状態」を更新する

---

## このプロジェクトの地図

| 場所 | 内容 |
|---|---|
| `web/` | 動くプロトタイプ（本番に近い体験） |
| `web/README.md` | 起動・Claude接続・Vercel |
| `docs/demo-survey.md` | Googleフォーム設問・ログイン不要設定 |
| `docs/git-setup.md` | Git の名前・メール設定（このPCで一度だけ） |
| `Logs/Discussion-2026-08-08.md` | 境界線・3人・エクスプレッシブ・ライティング |
| `Logs/Discussion-2026-08-12.md` | Why の設計（利用者向け／評価者向け） |
| `Logs/Discussion-2026-08-14.md` | 知人テスト対応・敬称ルール・相手を本人が選ぶ変更 |
| `Logs/Discussion-2026-08-15.md` | Why実例、誘導の指摘、「もう少し続ける」の設計 |
| `Logs/Discussion-2026-08-17.md` | 入口は場の約束（筋書きではない）。手書き・音声は将来 |
| `docs/WHY-ja.md` | 発表・商用向け Why（画面には出さない。仮説） |
| `docs/Discussion-2026-08-18-ja.md` | Why を固めた議論。入口文言の正ではない |
| `docs/evaluator-why-example.md` | 評価者に渡す実例1本 |
| `docs/reflective-ai-意義整理メモ.md` | 発表前の意義整理（素材メモ） |
| `docs/reflective_ai_design_doc.docx` | 設計メモ |
| `docs/open_dialogue_reflecting_academic_references.docx` | オープンダイアローグ関連の学術メモ |
| `docs/ChatGPTの見立て_260813.docx` / `docs/Claudeの見立て_260813.docx` | 汎用AIの見立て（個人メモを docs に置いたもの） |
| `docs/presentation-script-1min.md` | 卒業制作の1分進捗原稿 |
| `docs/presentation-script-6min-v2.md` | 6分発表の現行原稿。入口は場の約束として読む |
| `mockups/` / `slides/` | 発表・説明用の静的資料 |
| `.env.local`（`web/`内・Git対象外） | 秘密情報。GitHubに上げない |

### 文書の使い分け（全部を同時に正にしない）

作業の種類で、開くファイルを分ける。新しい議論が、入口の文言を上書きしない。

| やりたいこと | 正とするもの | 開かない／混ぜない |
|---|---|---|
| 入口画面の文言 | この HANDOFF の画面0 ＋ `Logs/Discussion-2026-08-17.md`（場の約束・筋書きを出さない） | `docs/WHY-ja.md` のコピー候補。効能を入口に置かない |
| 書く→リフレクト→ステージの構造 | `Logs/Discussion-2026-08-08.md` | 発表原稿の比喩（歯磨きなど）を画面に移植しない |
| 相手は本人が選ぶ・誘導しない | `Logs/Discussion-2026-08-14.md` / `08-15.md` | 候補を主ボタンにしない |
| 発表・「AIに相談するのと何が違うか」 | `docs/WHY-ja.md` / `docs/Discussion-2026-08-18-ja.md` / `docs/evaluator-why-example.md` | アプリ画面。Why は仮説 |
| 読み上げ原稿 | `docs/presentation-script-6min-v2.md` | アプリの実装ルール |
| URL・タブの説明 | `web/src/app/layout.tsx` の description。**変えない** | Why コピーで置き換えない |
| 感想フォーム | `docs/demo-survey.md` | 入口文言の変更だけでは触らない |

---

## いまの状態（手で更新する）

- ローカル: Next.js + Claude 接続できる状態まで実装済み
- デモ公開済み・知人テスト中
- Vercel: プロジェクト **`reflective-ai`**（Hobby）。URL は https://reflective-ai-red.vercel.app  
  **同じURLのまま更新する。** Dashboard で既存プロジェクトを開き、`main` へ push すると自動デプロイ。New Project は押さない（別URLになる）
- 環境変数: `AI_PROVIDER` / `ANTHROPIC_API_KEY` / `ANTHROPIC_MODEL` / `NEXT_PUBLIC_SURVEY_URL`
- Git: `main` は origin と同じ（2026-08-20、入口に誘いと「話してみる」を入れて push）
- URL説明（タブ・OGP）は変えない: 「判断やアドバイスをしない壁打ち。書いたことはここには保存されません。」（`web/src/app/layout.tsx`）
- Googleフォーム: 設問を現行アプリに合わせて更新済み。ログインなし送信を 2026-08-17 に確認
- 評価者向けWhyの実例: `docs/evaluator-why-example.md`。画面つき原文は `demo/0815夫-ChatGPTとの比較.docx`（未コミット）

### 現在のアプリの画面（2026-08-20 時点）

0. 入口: 「ここは、答えを出さない場所です。」＋「誰かに話したいこと、聞いてもらいたいことはありませんか？」＋「うれしかったことでも、もやもやでも。うまくまとめなくて大丈夫です。」＋「判断やアドバイスはしません。書いたことは保存されず、ほかの人に伝わることもありません。」／ボタン `話してみる`  
   筋書きは出さない。禁止事項の羅列もしない。Whyコピー（自分がどうしたいのかが見えてくる）も入口には出さない
1. 書く: 見出し「今、心にあることを書いてみませんか」＋「うれしかったことでも、もやもやでも。うまくまとめなくて大丈夫です。」  
   続ける途中で戻ったときは「足したいことがあれば、続きを書いてください。」  
   安心文「判断やアドバイスはしません。書いたことは応答をつくるためにAIへ送られますが、保存されず、ほかの人に伝わることもありません。」／ボタン `書きました`
2. リフレクト: 聞こえた人を並べるだけ。「ここで、相手を想定して話してみませんか。」  
   候補は**薄い紺**（例:「Sさんと話してみる」）。濃い紺の主ボタンにはしない  
   `もう少し続ける` → 書く画面に戻る（文は残る。AIは黙る。書きました、でもう一度だけ返す。前の返しは残る。2回目のあとはこのボタンは出ない）  
   `今日はここまでにする`  
   人が出てこなければ終了が主。呼び方を自分で書いて話せる（実体のない「その方」は出さない）  
   セッション中は「書いたことを見る」で、自分が書いた文を読める（保存ではない。終了したら消える）
3. ステージ: 相手役と話す。4往復か沈黙で  
   「今のやり取りを、少し離れたところから返してもらいませんか」（`返してもらう` / `もう少し話す`）
4. 終了: 「内容は保存されていません。話すことは、手放すことです。また書きたくなったら、いつでも来てください。」＋ 感想フォーム  
   通知・リマインドは入れない

### 決まっている設計ルール（変えるときは経緯を確認する）

- 呼び方: リフレクターは「〜さん」に寄せる（泉／泉ちゃん → 泉さん、娘 → 娘さん、義母 → お義母さん）。お父さん・妻・夫・上司・役職はそのまま。「夫さん」は誤り。**ステージの相手役は原文どおり**
- 誰に話すかをAIが決めない（対象を見極めるのは本人の工程）。候補を濃い紺の主ボタンにしない（誘導になる）
- リフレクターとは会話しない。一度だけ返す。`もう少し続ける` は書くことであり、左に入力して対話することではない
- 効能を約束しない。「ゆっくり」「じっくり」「時間をかけて」は使わない（速度の話ではない）
- 通知・リマインドは入れない。会を重ねる戻り方は終了画面の「いつでも来てください」だけ。保存して積み重ねるオープンダイアローグそのものにはしない
- ロゴマークは画面に置かない（タブ用の「R」円だけ）。タグライン「話す前に、少し書く。」も画面には出さない
- 入口は場の約束（「ここは、答えを出さない場所です」）と誘い。進行の筋書きは出さない。禁止事項の羅列もしない。08-18 の Why は発表用であり、入口には置かない
- URL説明（metadata）は「判断やアドバイスをしない壁打ち。書いたことはここには保存されません。」のまま。Why と混ぜない
- 書いた文はセッション中だけ読める。「保存しない」は預からないこと。会をまたいで残さない。スクショを勧めない
- ステージ途中の誘い: 「今のやり取りを、少し離れたところから返してもらいませんか」。「話を聞いてみませんか」「ここで一度」は使わない（誰の話か曖昧／くどい）
- リフレクト本文はおおよそ3〜4文。引用だけで終わらず、伝わってきたものを一文添える。「私はこう思う」は付けない。「印象に残っています」は使ってよいが、同じ結びを続けない
- 汎用AIでも返す文は書ける。差は返して止まること（`docs/evaluator-why-example.md`）

### 次にやること

1. 知人テストを続ける。感想は公開中のフォームへ（ログイン不要）

### 止めてある話（言葉になるまで触らない）

リフレクトを続けていると「対話の整理」のように感じる、という観察。名前は置いていない。Reflective AI 特有か、最適解を取るためにAIへ語り掛ける癖なのか、未決。

### 未追跡のまま置いてあるもの（意図的）

`demo/*.docx`、`ReflectiveAIの選択肢改善.pdf`（知人テストの原文・家族の固有名が入る個人メモのため、GitHub には上げない）

---

## ファイル名と置き場所について

- **名前:** `HANDOFF.md`（「引継ぎ」と一目で分かる）
- **場所:** リポジトリの一番上（ルート）  
  → 個別機能のフォルダより、再開時に探しやすい
- **GitHubに入れる:** よい（チーム・未来の自分・次のチャット用）。秘密は書かない
- README の File Map からこのファイルへリンクしておくとさらに見つけやすい
